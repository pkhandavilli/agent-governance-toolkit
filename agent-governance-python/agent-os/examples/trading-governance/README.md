# Trading governance audit artifacts

The retired local rule-engine demo was removed. The remaining JSON file is historical sample audit output. Use native ACS manifests under `examples/policies/production/`.
