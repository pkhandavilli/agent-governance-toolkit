# Finance SOC 2 audit artifacts

The retired local rule-engine demo was removed. The remaining files are historical sample audit output. Use the native ACS profiles under `examples/policies/production/`.
