# AML fraud detection audit artifacts

The retired local rule-engine demo was removed. The remaining CSV and JSON files are historical sample audit output. Use `examples/policies/production/financial.yaml` for a native ACS financial profile.
